-- Single-monitor setup (eDP-1 only). Regenerate with nwg-displays if hardware changes.
-- Original: workspace=1,monitor:eDP-1,default:true

hl.workspace_rule({
  workspace = "1",
  monitor = "eDP-1",
  default = true,
})
