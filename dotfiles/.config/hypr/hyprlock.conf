# By: Felipe Avelar
# ~/.config/hypr/hyprlock.conf


# BACKGROUND
background {
    monitor =
    path = ~/Pictures/Wallpapers/angel.png
    blur_passes = 3
    contrast = 1
    brightness = 0.7
    vibrancy = 0
    vibrancy_darkness = 0.0
}

animations {
    enabled = false
}

# INPUT FIELD
input-field {
    monitor = eDP-1
    size = 250, 60
    outline_thickness = 2
    rounding = 0
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgb(ffffff)
    inner_color = rgb(1b1b1b)
    font_color = rgb(ffffff)
    fade_on_empty = false
    font_family = SF Pro
    placeholder_text = <i><span foreground="##ffffff">Password...</span></i>
    hide_input = false
    position = 0, -120
    halign = center
    valign = center
}

# TIME
label {
    monitor = eDP-1
    text = cmd[update:1000] echo "$(date +"%I:%M %p")"
    color = rgb(ffffff)
    font_size = 120
    font_family = "SF Pro"
    position = 0, -500
    halign = center
    valign = top
}

# USER
label {
    monitor = eDP-1
    text = Hello, Felipe.
    color = rgb(ffffff)
    font_size = 25
    font_family = SF Pro
    position = 0, -40
    halign = center
    valign = center
}
